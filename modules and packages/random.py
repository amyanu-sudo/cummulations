from random import randint,choice
random_integer = randint(1, 10)
#prints the random integer between 1 and 10
print(random_integer)
random_ele = choice(["A","B","C"])
#prints the random alphabet.
print(random_ele)
